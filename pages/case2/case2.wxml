<view>
<swiper indicator-dots="{{indicatorDots}}" autoplay="{{true}}" interval="{{interval}}" duration="{{duration}}">
	<block wx:for="{{imgs}}">
		<swiper-item>
	        <image src="{{item}}"/>
		</swiper-item>
	</block>
</swiper>
</view>